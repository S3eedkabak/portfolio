import React,{useMemo,useRef}from"react";import{Canvas,useFrame}from"@react-three/fiber";import{Html,useGLTF,useProgress}from"@react-three/drei";import * as THREE from"three";
const MODEL_URL="https://raw.githubusercontent.com/shahdinsalman23/react-macbookpro/main/public/mac.glb";
function Loader(){const{progress}=useProgress();return <Html center><div style={{font:"9px 'DM Mono',monospace",letterSpacing:".14em",color:"#d9ff4f",whiteSpace:"nowrap"}}>LOADING MACHINE {Math.round(progress)}%</div></Html>}
function Model({progress}){const{scene}=useGLTF(MODEL_URL);const root=useRef();const parts=useMemo(()=>{const clone=scene.clone(true);const screen=clone.getObjectByName("screen");return{clone,screen,screenBase:screen?.position.clone()||new THREE.Vector3(),screenRotation:screen?.rotation.clone()||new THREE.Euler()}},[scene]);useFrame(()=>{const p=THREE.MathUtils.clamp(progress,0,1);const open=THREE.MathUtils.smoothstep(p,0,.5);const lift=THREE.MathUtils.smoothstep(p,.5,.9);if(parts.screen){parts.screen.position.copy(parts.screenBase);parts.screen.rotation.set(THREE.MathUtils.degToRad(180-90*open),parts.screenRotation.y,parts.screenRotation.z)}if(root.current){root.current.position.set(0,-10+lift*1.2,20);root.current.rotation.set(0,0,0);root.current.scale.setScalar(1.05+lift*.04)}});return <primitive ref={root} object={parts.clone}/>}
useGLTF.preload(MODEL_URL);
export default function MacBookScene({progress}){return <Canvas className="mac-canvas" camera={{fov:12,position:[1,-10,220]}} dpr={[1,1.5]} gl={{antialias:true,alpha:true}} onCreated={({camera})=>camera.lookAt(0,0,0)}><ambientLight intensity={1.7}/><hemisphereLight intensity={1.2}/><directionalLight position={[5,7,20]} intensity={3}/><directionalLight position={[-6,4,12]} intensity={1.5}/><pointLight position={[0,5,25]} intensity={1.2} color="#d9ff4f"/><React.Suspense fallback={<Loader/>}><Model progress={progress}/></React.Suspense></Canvas>}